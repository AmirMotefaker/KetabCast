# v0.2.0-beta.4.1 — New-Three Factory Batch

Issue: #45
Tracks: #8

## Batch
`new-three` resolves exactly:
1. think-again
2. zero-to-one
3. leading-teams

## Coverage
The same selection contract is used by:
- content generation
- audio manifest
- GitHub Release Asset upload
- production metadata promotion
- workflow audio loop / expected-count logic

## Publication lifecycle
The workflow now accepts a linked `release_issue` input so the automated
production PR can close a dedicated release Issue instead of relying only on
the parent roadmap Issue.

## Integrity
- production MP3 mutation: NO
- `src/content/episodes.json` mutation: NO
- factory catalog mutation: NO
- existing Atomic Habits / Deep Work evidence mutation: NO
